项目概述
开发一款基于 Tauri 框架的桌面端正念效率应用，融合“任务清单”、“正念微日记”与“沉浸式定时觉察”功能，旨在帮助用户兼顾日常事务推进与心理觉察。

技术栈选型
核心框架： Tauri (Rust 后端 + Web 前端，支持打包为 Windows/macOS/Linux 桌面应用)

前端： React / Vue 3 + Tailwind CSS（构建UI、动态交互、裂屏 Canvas/WebGL 特效）

后端 (Rust)： 处理系统托盘、多显示器全屏窗口控制、本地文件读写（JSON/SQLite）及全局快捷键监听

核心功能模块
1. 双轨制任务与日记系统
类别一：正念微日记卡片

标题引导：“今天，哪一个瞬间让我感到纯粹的充实、专注或者快乐？”

交互：输入感更强的记录框，输入文字后自动收录为当天的美好瞬间卡片。

标签支持：支持在输入时通过 #标签（如 #工作、#灵感、#运动）进行轻量标记。

类别二：日常事务清单

标题引导：“今天完成了什么日常事务？”

交互：传统 Checklist 形式，点击打勾后触发 CSS 删除线 (text-decoration: line-through) 划掉效果。

2. 沉浸式倒计时与觉察提醒
时间自定义： 用户可在设置中自定义总倒计时长（如 20 分钟）、提前多久预警（如最后 2 分钟）以及最后强提醒倒数（如最后 10 秒）。

智能防干扰（AFK 离开检测）： 引入系统空闲检测。若用户在倒计时期间处于离开状态（无键盘鼠标操作），到达时间时自动静默延后或跳过强提醒，避免空座位触发干扰。

触发与窗口表现：

默认状态下，倒计时悬浮窗隐藏且不阻挡底层鼠标点击（通过 ignore_cursor_events 实现鼠标穿透）。

到达预警时间节点时，悬浮窗口瞬间变为可见、聚焦并拦截鼠标事件。

多显示器全屏覆盖： 动态获取所有屏幕尺寸，全屏裂开特效与大字提示铺满所有连接的显示器屏幕。

视觉与听觉震撼：

视觉： 整个屏幕正中央以超大字号展示提示：“请总结已经完成的事和还没完成的事”，并伴随全屏碎屏/裂开特效（通过 CSS 动画、Canvas 或 WebGL 渲染）。

听觉： 播放温和且有质感的音频（如颂钵回响、木质风铃、微风音效，或支持用户自定义上传音频文件），且可在设置中调节音量或关闭。

输入与防劫持（Snooze 机制）：

界面提供一个即时填写的文本框（用户无需点击保存，系统自动静默落盘）。

提供显眼的“稍后处理（Snooze 5分钟）”或“结束本次专注”按钮，同时支持全局快捷键（如 Esc 或 Space），防止软件强行“绑架”用户屏幕。

3. 历史回顾与时间轴
提供“历史回顾（Timeline / Calendar）”页面。

支持翻阅过去几天记录的美好瞬间卡片与已完成事务，并支持按 #标签 进行多维筛选。

4. 数据安全与隐私保护
隐私安全锁： 设置中提供隐私锁选项（支持简单数字密码锁或一键“隐藏敏感内容”的全局快捷键/Boss Key）。开启后，微日记内容自动进行毛玻璃模糊化处理。

数据导出： 支持将本地数据一键导出为 Markdown 或 CSV 格式，确保用户对个人心灵反思数据的绝对掌控。

实时容灾防丢： 前端实现实时自动草稿缓存（Auto-Save / LocalStorage 防丢机制），防止因意外崩溃导致正在输入的微日记文本丢失。

5. 系统托盘与基础交互
应用支持最小化或关闭主窗口后常驻右下角系统托盘（System Tray）。

点击托盘图标可快速呼出/隐藏主界面。

所有配置参数（倒计时长、提醒阈值、音频路径、密码等）持久化保存在本地配置文件中，每次启动自动读取。